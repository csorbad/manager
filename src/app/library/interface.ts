export interface Book {
  id: string;
  title: string;
  author: string;
  publisher: string;
  year?: string;
  description?: string;
  isbn?: string;
  series?: string;
  img?: string;
  cover?: string;
}
